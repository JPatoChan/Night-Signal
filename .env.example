# Environment configuration example

# NASA API key for Special Signal's Near-Earth Object data (api.nasa.gov).
# Optional: falls back to NASA's public "DEMO_KEY" if not set, which works
# without registration but is more tightly rate-limited.
NASA_API_KEY=
